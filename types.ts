export type UserInput = {
  name: string;
  username: string;
  email: string;
  city: string;
};

export type User = {
  id: number;
  name: string;
  username: string;
  email: string;
  city: string;
};
